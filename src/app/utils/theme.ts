import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff5000',
      contrastText: '#fff',
    },
    secondary: {
      main: '#4a148c',
      contrastText: '#fff',
    },
  },
});

export default theme;
